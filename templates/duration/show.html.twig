{% extends 'base.html.twig' %}

{% block title %}Duration{% endblock %}

{% block body %}
    <h1>Duration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ duration.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ duration.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_duration_index') }}">back to list</a>

    <a href="{{ path('app_duration_edit', {'id': duration.id}) }}">edit</a>

    {{ include('duration/_delete_form.html.twig') }}
{% endblock %}
