{% extends 'base.html.twig' %}

{% block title %}Duration index{% endblock %}

{% block body %}
    <h1>Duration index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for duration in durations %}
            <tr>
                <td>{{ duration.id }}</td>
                <td>{{ duration.name }}</td>
                <td>
                    <a href="{{ path('app_duration_show', {'id': duration.id}) }}">show</a>
                    <a href="{{ path('app_duration_edit', {'id': duration.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_duration_new') }}">Create new</a>
{% endblock %}
