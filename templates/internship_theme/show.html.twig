{% extends 'base.html.twig' %}

{% block title %}InternshipTheme{% endblock %}

{% block body %}
    <h1>InternshipTheme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internship_theme.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ internship_theme.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_internship_theme_index') }}">back to list</a>

    <a href="{{ path('app_internship_theme_edit', {'id': internship_theme.id}) }}">edit</a>

    {{ include('internship_theme/_delete_form.html.twig') }}
{% endblock %}
