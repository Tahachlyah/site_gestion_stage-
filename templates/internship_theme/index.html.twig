{% extends 'base.html.twig' %}

{% block title %}InternshipTheme index{% endblock %}

{% block body %}
    <h1>InternshipTheme index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for internship_theme in internship_themes %}
            <tr>
                <td>{{ internship_theme.id }}</td>
                <td>{{ internship_theme.name }}</td>
                <td>
                    <a href="{{ path('app_internship_theme_show', {'id': internship_theme.id}) }}">show</a>
                    <a href="{{ path('app_internship_theme_edit', {'id': internship_theme.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_internship_theme_new') }}">Create new</a>
{% endblock %}
